# Task Completion Checklist

When a task is completed, run the following:

1. **Format**: `cargo fmt`
2. **Lint**: `cargo clippy`
3. **Test**: `cargo test -p game-plugin`
4. **Build check**: `cargo build` (ensure no compilation errors)

All tests must pass before considering a task done.
